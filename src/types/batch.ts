/**
 * Batch type definitions for the application
 * Re-exports from batches-context to maintain single source of truth
 */

export type { Batch, BatchStatus, BatchHistoryEvent } from "@/contexts/batches-context";
